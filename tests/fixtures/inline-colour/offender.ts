/**
 * The negative control for tools/style-tokens.ts `checkColourLiterals`. Not application code — it
 * exists so the "only the email theme may spell a colour out" rule can be seen to fail.
 */
export const BRAND = '#ff00aa';
export const OVERLAY = 'rgba(0, 0, 0, 0.4)';
