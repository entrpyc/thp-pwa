/**
 * A module that spells a book of the Bible instead of asking the canon for it. Never imported —
 * tests/guards/domain-declarations.test.ts points the book-name check at this directory to prove
 * the check fails on something, which is the only thing that makes it worth running.
 */
export const HEADING = 'John 3:16';
