import { Library } from './library';

export const dynamic = 'force-dynamic';

/** `/recordings` — the library. The layout checks the session; this is the composition. */
export default function MemberLibraryPage() {
  return <Library />;
}
